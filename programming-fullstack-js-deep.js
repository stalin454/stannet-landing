(()=>{const J={
'JavaScript runtime':['Qué es JavaScript y cómo se ejecuta','JavaScript es un lenguaje de programación de alto nivel usado por el navegador y otros runtimes como Node.js. El motor analiza y ejecuta el código; en el navegador, las Web APIs aportan temporizadores, DOM y red. JavaScript ejecuta una tarea de código a la vez en su hilo principal, mientras el entorno coordina operaciones asíncronas.','console.log("Primera instrucción");\nconsole.log("Segunda instrucción");','Explica con tus palabras la diferencia entre lenguaje, motor JavaScript y APIs del navegador.'],
'variables let const':['Variables: nombres para valores','Una variable permite referenciar un valor. const impide reasignar el binding y debe ser la opción habitual cuando el nombre no cambia; let permite reasignación. Ambos tienen scope de bloque. const no vuelve inmutable un objeto: sus propiedades aún pueden cambiar.','const nombre = "Stan";\nlet puntos = 0;\npuntos = puntos + 1;','Crea tres datos de un usuario y decide justificadamente cuáles declarar con const y cuál con let.'],
'types':['Tipos y valores','JavaScript tiene valores primitivos como string, number, bigint, boolean, undefined, symbol y null, además de objetos. typeof ayuda a inspeccionar tipos, aunque typeof null devuelve "object" por una peculiaridad histórica. Comprender tipos evita operaciones inesperadas y ayuda a validar entradas.','const edad=52;\nconst activo=true;\nconsole.log(typeof edad, typeof activo);','Declara ejemplos de cada primitivo habitual y comprueba typeof.'],
'operators':['Operadores y expresiones','Los operadores combinan valores para producir otros valores: aritméticos (+ - * / % **), comparación (< > <= >=), igualdad, lógicos (&& || !), asignación y otros. La precedencia determina qué operación se evalúa primero; usa paréntesis cuando mejoren claridad.','const total = 10 * 3 + 5;\nconst permitido = total >= 30 && total < 100;','Construye una condición de acceso usando al menos un operador aritmético, uno de comparación y uno lógico.'],
'strings and template literals':['Texto y plantillas','Los strings representan texto. Las template literals usan backticks y permiten interpolar expresiones con ${...}, además de texto multilínea. Métodos como trim, includes, slice, toLowerCase o replace transforman o consultan texto.','const nombre="Stan";\nconst mensaje = `Hola, ${nombre}. Tienes ${2+3} tareas.`;','Genera una ficha de usuario con template literals y transforma una entrada con trim y toLowerCase.'],
'coercion and equality':['Conversión, coerción e igualdad','JavaScript puede convertir tipos explícita o implícitamente. Number(), String() y Boolean() hacen la intención visible. == permite coerción y puede producir comparaciones sorprendentes; === compara sin convertir tipos y suele ser la opción segura. NaN representa un resultado numérico inválido y se comprueba con Number.isNaN.','console.log("5" == 5); // true\nconsole.log("5" === 5); // false\nconsole.log(Number("5") + 1);','Predice y después comprueba cinco comparaciones con == y ===; explica cada resultado.'],
'conditionals if else':['Decisiones con if / else','Un condicional decide qué bloque ejecutar. if evalúa una condición; else if permite alternativas y else cubre el resto. La condición se interpreta como truthy o falsy. Diseña ramas mutuamente comprensibles y evita anidación innecesaria.','const edad=20;\nif(edad>=18){ console.log("adulto"); } else { console.log("menor"); }','Crea un clasificador de nota con varias ramas y prueba valores límite.'],
'switch':['Selección con switch','switch compara una expresión con varios case usando igualdad estricta. break evita continuar accidentalmente hacia el siguiente case; default cubre valores no contemplados. Es útil cuando una misma expresión tiene alternativas discretas.','const rol="editor";\nswitch(rol){case "admin": console.log("todo"); break; case "editor": console.log("editar"); break; default: console.log("leer");}','Implementa un menú de acciones con switch, break y default.'],
'ternary operator':['Condición como expresión','El operador ternario condición ? valorSiTrue : valorSiFalse produce un valor y funciona bien para decisiones pequeñas. No sustituye a estructuras complejas: ternarios anidados suelen reducir legibilidad.','const edad=20;\nconst acceso=edad>=18 ? "permitido" : "denegado";','Reescribe un if/else simple como ternario y explica cuál versión resulta más clara.'],
'loops fundamentals':['Qué es un bucle','Un bucle repite instrucciones. Todo bucle necesita entender tres ideas: estado inicial, condición de continuidad y cambio de estado. Si la condición nunca llega a ser falsa puede aparecer un bucle infinito. Elegir el bucle depende de si conocemos el número de iteraciones o recorremos una colección/condición.','let i=0;\nwhile(i<3){ console.log(i); i++; }','Antes de programar, identifica inicialización, condición, cuerpo y actualización de un bucle que repita cinco veces.'],
'for loop':['Bucle for: repetición controlada','for reúne inicialización, condición y actualización en la cabecera: for(inicio; condición; cambio). Es ideal cuando el contador o número de iteraciones forma parte natural del problema. La condición se comprueba antes de cada vuelta.','for(let i=0;i<5;i++){\n  console.log("Vuelta",i);\n}','Imprime del 1 al 10, después solo pares y finalmente calcula la suma 1..100. Explica cada parte del for.'],
'while loop':['Bucle while: mientras se cumpla una condición','while comprueba una condición antes de cada iteración y repite mientras sea truthy. Es apropiado cuando no sabemos de antemano cuántas vueltas harán falta, por ejemplo reintentos o procesamiento hasta alcanzar un estado. Debes garantizar progreso hacia la salida.','let saldo=30;\nwhile(saldo>=10){\n console.log("Compra"); saldo-=10;\n}','Simula intentos hasta alcanzar un objetivo y explica por qué while es más natural que for.'],
'do while loop':['do...while: ejecutar al menos una vez','do...while ejecuta primero el cuerpo y comprueba la condición después. Por eso siempre hay al menos una iteración. Encaja en menús o procesos donde la primera ejecución debe ocurrir antes de decidir si repetir.','let intento=0;\ndo { intento++; console.log("Intento",intento); } while(intento<3);','Construye un menú simulado que se ejecute una vez aunque la condición inicial sea falsa.'],
'for of':['for...of: recorrer valores iterables','for...of recorre los valores de un iterable como Array, String, Map o Set. Evita gestionar manualmente un índice cuando solo necesitas cada valor. No es el bucle para enumerar propiedades generales de un objeto.','for(const nombre of ["Ana","Luis","Marta"]){ console.log(nombre); }','Recorre un array de precios, muestra cada valor y calcula el total.'],
'for in':['for...in: recorrer claves enumerables','for...in recorre nombres de propiedades enumerables de un objeto. En arrays normalmente preferimos for...of o métodos de array porque for...in trabaja con claves y puede incluir propiedades heredadas enumerables.','const user={nombre:"Stan",rol:"student"};\nfor(const clave in user){ console.log(clave,user[clave]); }','Recorre un objeto de configuración y explica por qué no elegirías for...in para una lista normal.'],
'break and continue':['Controlar una iteración','break termina el bucle completo inmediatamente. continue abandona solo la iteración actual y pasa a la siguiente. Son herramientas útiles cuando expresan claramente una condición de salida o descarte, aunque un exceso puede volver difícil seguir el flujo.','for(let i=1;i<=10;i++){\n if(i===3) continue;\n if(i===8) break;\n console.log(i);\n}','Recorre 1..20, omite múltiplos de 3 y detente al llegar a 17; explica cada salto.'],
'nested loops':['Bucles anidados y coste','Un bucle dentro de otro ejecuta el interior por cada iteración exterior. Es útil para matrices, combinaciones o tablas, pero puede multiplicar el trabajo: dos recorridos de n elementos suelen implicar aproximadamente n² operaciones.','for(let fila=1;fila<=3;fila++){\n for(let col=1;col<=3;col++) console.log(fila,col);\n}','Genera una tabla 3x3 y cuenta cuántas veces se ejecuta el cuerpo interior.'],
'functions':['Funciones: comportamiento reutilizable','Una función encapsula comportamiento, recibe parámetros y puede devolver un resultado con return. Diferencia declaración, expresión y arrow function; busca funciones pequeñas con una responsabilidad clara. Un return finaliza la ejecución de esa llamada.','function sumar(a,b){ return a+b; }\nconst doble=n=>n*2;','Crea funciones para validar un precio, calcular IVA y producir un total sin usar variables globales.'],
'callbacks':['Callbacks: funciones como valores','En JavaScript una función es un valor: puede pasarse a otra función. Un callback es una función entregada para que otra operación la invoque. Aparece en eventos, métodos de arrays y APIs asíncronas. No significa automáticamente asincronía.','function procesar(valor,fn){ return fn(valor); }\nconsole.log(procesar(5,n=>n*2));','Escribe una función calcular(a,b,operacion) y pásale callbacks para sumar y multiplicar.'],
'scope':['Scope: dónde existe un nombre','El scope determina desde qué partes del programa puede accederse a una variable. let y const respetan bloques; una función crea su propio scope. La búsqueda léxica permite a código interior consultar nombres exteriores. Evita globales accidentales.','const global="A";\nfunction demo(){ const local="B"; if(true){ const bloque="C"; console.log(global,local,bloque); }}','Predice qué variables son visibles en tres niveles de bloques y verifica el resultado.'],
'closures':['Closures: recordar el entorno léxico','Una closure aparece cuando una función conserva acceso a variables del entorno donde fue creada incluso después de que ese entorno haya terminado su ejecución. Permite encapsular estado, factories y callbacks configurados.','function crearContador(){ let n=0; return ()=>++n; }\nconst contar=crearContador();\nconsole.log(contar(),contar());','Construye una factory crearMultiplicador(factor) y explica qué variable queda capturada.'],
'recursion':['Recursividad: una función se llama a sí misma','Una función recursiva resuelve un problema reduciéndolo a una versión menor. Necesita un caso base que detenga las llamadas y un paso recursivo que avance hacia él. Sin caso base puede desbordar la pila.','function factorial(n){ if(n<=1) return 1; return n*factorial(n-1); }','Traza factorial(4) llamada por llamada y crea una cuenta regresiva recursiva con caso base.'],
'arrays':['Arrays: colecciones ordenadas','Un array almacena una secuencia indexada desde cero. length indica tamaño. push/pop modifican el final; shift/unshift el inicio; slice copia una sección y splice modifica la colección. Distingue métodos mutables de los que producen nuevos valores.','const items=["a","b"];\nitems.push("c");\nconsole.log(items[0],items.length);','Crea una lista, añade/elimina elementos y demuestra la diferencia entre slice y splice.'],
'array methods map filter find reduce':['map, filter, find y reduce','map transforma cada elemento y devuelve un array del mismo número de posiciones; filter conserva los que cumplen un predicado; find devuelve el primer elemento coincidente; reduce acumula toda la colección en un resultado. Elige por intención, no para evitar for a toda costa.','const nums=[1,2,3,4];\nconsole.log(nums.map(n=>n*2));\nconsole.log(nums.filter(n=>n%2===0));\nconsole.log(nums.find(n=>n>2));\nconsole.log(nums.reduce((a,n)=>a+n,0));','Con una cesta de productos: filtra disponibles, transforma nombres, encuentra uno por id y calcula total con reduce.'],
'objects':['Objetos: datos por propiedades','Un objeto agrupa propiedades clave-valor. Accede con punto cuando conoces el nombre y corchetes cuando la clave es dinámica. Los objetos se manejan por referencia; copiar una variable no clona el objeto. Object.keys/values/entries ayudan a recorrer datos propios.','const user={name:"Stan",skills:["JS"]};\nconsole.log(user.name);\nconsole.log(Object.entries(user));','Modela un usuario con datos anidados y accede a una propiedad mediante una clave calculada.'],
'destructuring and spread':['Desestructuración y spread','La desestructuración extrae valores de arrays u objetos con sintaxis declarativa. Spread (...) expande elementos o propiedades y permite copias superficiales y composición. Una copia con spread no clona profundamente objetos anidados.','const user={name:"Stan",role:"student"};\nconst {name,...rest}=user;\nconst updated={...user,role:"developer"};','Actualiza de forma no mutante una propiedad y demuestra qué ocurre con un objeto anidado.'],
'DOM':['DOM: la página como árbol de objetos','El DOM representa el documento HTML como nodos manipulables desde JavaScript. querySelector localiza elementos; textContent cambia texto; createElement crea nodos; append los inserta. Prefiere APIs seguras y evita innerHTML con contenido no confiable.','const h=document.querySelector("h1");\nif(h) h.textContent="Hola desde JavaScript";','Crea mediante JS una lista de tres elementos y añádela a un contenedor existente.'],
'events':['Eventos: reaccionar al usuario','Los eventos describen acciones como click, input, submit o keydown. addEventListener registra handlers. Los eventos se propagan por el árbol; preventDefault cancela la acción predeterminada cuando procede y delegation aprovecha bubbling para listas dinámicas.','button.addEventListener("click",()=>console.log("click"));','Crea un formulario que capture submit sin recargar y valide un campo antes de mostrar resultado.'],
'modules':['Módulos: separar responsabilidades','Los módulos ES usan export/import para declarar dependencias explícitas y scopes separados. Divide por responsabilidad y evita archivos gigantes o ciclos. En navegador se cargan con type="module".','export const sumar=(a,b)=>a+b;\n// otro archivo: import {sumar} from "./math.js";','Separa una pequeña aplicación en módulo de cálculos y módulo de interfaz.'],
'errors':['Errores y manejo de fallos','throw crea/propaga un error. try/catch permite manejar fallos esperados en una frontera adecuada y finally ejecuta limpieza. No captures errores para ignorarlos: añade contexto o presenta una recuperación clara.','function dividir(a,b){ if(b===0) throw new Error("División por cero"); return a/b; }\ntry{ dividir(1,0); }catch(e){ console.error(e.message); }','Valida una entrada y distingue error de datos inválidos de un resultado válido.'],
'promises':['Promises: un resultado futuro','Una Promise representa una operación que puede quedar pending y terminar fulfilled o rejected. then procesa éxito, catch fallo y finally finalización. Encadenar retornando valores/promesas evita pirámides y mantiene propagación de errores.','Promise.resolve(5).then(n=>n*2).then(console.log).catch(console.error);','Crea una Promise que resuelva después de un temporizador y encadena una transformación.'],
'async await':['async/await sobre Promises','Una función async siempre devuelve una Promise. await pausa esa función hasta que una Promise se resuelva o rechace, sin bloquear todo el runtime. Usa try/catch cuando puedas recuperar o contextualizar el fallo. Operaciones independientes pueden ejecutarse concurrentemente con Promise.all.','async function main(){ try{ const v=await Promise.resolve(42); console.log(v); }catch(e){ console.error(e); }}','Compara dos esperas secuenciales con Promise.all y explica cuándo cada estrategia es correcta.'],
'fetch':['Fetch y HTTP desde JavaScript','fetch devuelve una Promise de Response. Un 404/500 normalmente no rechaza la Promise, por lo que debes comprobar response.ok. Después parseas el cuerpo, por ejemplo con json(). Considera loading, error, cancelación y validación de datos.','async function getJSON(url){ const r=await fetch(url); if(!r.ok) throw new Error(`HTTP ${r.status}`); return r.json(); }','Consume una API pública de prueba, controla !ok y muestra loading, éxito y error.'],
'event loop':['Event loop: orden de ejecución','El call stack ejecuta JavaScript síncrono. Cuando operaciones asíncronas terminan, sus callbacks continúan mediante colas. Las microtasks de Promises se procesan antes de la siguiente task típica como setTimeout, una vez vacía la pila actual. Este modelo explica órdenes que parecen extraños.','console.log("A");\nsetTimeout(()=>console.log("B"),0);\nPromise.resolve().then(()=>console.log("C"));\nconsole.log("D"); // A D C B','Predice el orden de varios console.log con Promise y setTimeout antes de ejecutarlos.'],
'OOP':['Objetos, clases y prototipos','JavaScript usa herencia prototípica. class ofrece una sintaxis cómoda para constructores, métodos, extends y super, pero debajo sigue existiendo la cadena de prototipos. Favorece composición cuando simplifique dependencias.','class User{ constructor(name){this.name=name} greet(){return `Hola ${this.name}`} }','Crea una clase base y una derivada; después resuelve el mismo problema con composición y compara.'],
'functional patterns':['Patrones funcionales','Programación funcional favorece funciones puras, composición, datos tratados de forma inmutable y transformación declarativa. Una función pura devuelve el mismo resultado para las mismas entradas y evita efectos externos observables. No toda operación puede ser pura: separa efectos de lógica.','const aplicarIVA=(precio,tasa)=>precio*(1+tasa);\nconst total=items=>items.reduce((s,x)=>s+x,0);','Separa cálculo puro de una cesta del código que lee DOM y muestra el resultado.'],
'testing':['Testing de JavaScript','Una prueba automatizada prepara un escenario, ejecuta comportamiento y verifica un resultado. Cubre happy path, límites y fallos. Las funciones puras suelen ser fáciles de probar; separarlas de DOM/red mejora diseño. Un test debe fallar por una razón útil.','function sumar(a,b){return a+b;}\nconsole.assert(sumar(2,3)===5,"2+3 debe ser 5");','Diseña casos para una función de descuento: normal, cero, límite, entrada inválida y explica qué protege cada test.']
};
const old=window.stannetTextbook;
window.stannetTextbook={...old,expand(topic,phase){const b=old.expand(topic,phase),x=phase==='FS03'&&J[topic];if(!x)return b;return {...b,overview:x[1],chapterSections:[[x[0],x[1]],['Sintaxis y ejemplo',x[2]]],guidedLab:x[3],challenge:'Resuelve una variante sin copiar el ejemplo. Predice primero el resultado, ejecuta después y explica por qué ocurre.',definitions:[['concepto',x[0]],['objetivo','Poder escribirlo, ejecutarlo, depurarlo y explicarlo en una entrevista técnica.']],mistakes:['Copiar código sin poder explicar el flujo de ejecución.','Probar solo el caso feliz y olvidar límites o errores.','Elegir una construcción por costumbre en vez de por intención.'],mastery:['Definir el concepto con tus palabras.','Escribir un ejemplo desde cero.','Predecir el resultado antes de ejecutar.','Depurar una variante con un error.','Explicar cuándo usarlo y cuándo elegir otra alternativa.'],practice:[x[3],'Modifica el ejemplo y predice el nuevo resultado.','Introduce un fallo deliberado, localízalo y corrígelo.'],checkQuestions:['¿Qué problema resuelve '+topic+'?','¿Cómo se ejecuta paso a paso?','¿Qué error típico cometería un principiante?','¿En qué caso real de una web lo utilizarías?','¿Cómo demostrarías que funciona correctamente?'],completion:'No marques la lección como dominada hasta poder programarla sin copiar y explicar cada línea.'};}};
})();