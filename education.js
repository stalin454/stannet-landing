const educationKey = 'stannet.education.records';
const grid = document.querySelector('#educationGrid');
const form = document.querySelector('#educationForm');
document.querySelectorAll('.filter-button').forEach((button) => button.addEventListener('click', () => { document.querySelectorAll('.filter-button').forEach((item) => item.classList.remove('active')); button.classList.add('active'); const filter = button.dataset.filter; document.querySelectorAll('.education-card').forEach((card) => { card.hidden = filter !== 'all' && card.dataset.type !== filter; }); }));
function escapeText(value) { return String(value ?? '').replace(/[&<>"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char])); }
function renderRecords() { grid.querySelectorAll('[data-record]').forEach(card => card.remove()); JSON.parse(localStorage.getItem(educationKey) || '[]').forEach((record) => { const card = document.createElement('article'); card.className = 'education-card'; card.dataset.record = 'true'; card.dataset.type = record.kind; card.innerHTML = `<span class="card-number">NUEVO / ${escapeText(record.kind.toUpperCase())}</span><div class="education-icon">▣</div><h3>${escapeText(record.title)}</h3><p class="education-meta">${escapeText(record.issuer || 'Registro personal')} · ${escapeText(record.year || 'Sin año')}</p><p>${record.fileName ? `Evidencia: ${escapeText(record.fileName)}` : 'Registro añadido a tu biblioteca personal.'}</p>`; grid.append(card); }); }
form?.addEventListener('submit', (event) => { event.preventDefault(); const data = new FormData(form); const records = JSON.parse(localStorage.getItem(educationKey) || '[]'); records.push({ title: data.get('title'), kind: data.get('kind'), year: data.get('year'), issuer: data.get('issuer'), fileName: data.get('file')?.name || '' }); localStorage.setItem(educationKey, JSON.stringify(records)); form.reset(); document.querySelector('#uploadMessage').textContent = 'Registro guardado en este navegador.'; document.querySelector('#uploadMessage').classList.add('show'); renderRecords(); document.querySelector('.filter-button.active')?.click(); });
renderRecords();
