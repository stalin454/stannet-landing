(()=>{const input=document.querySelector('#audioFile'),drop=document.querySelector('#dropZone'),pick=document.querySelector('#pickFile'),panel=document.querySelector('#playerPanel'),player=document.querySelector('#audioPlayer'),name=document.querySelector('#fileName'),meta=document.querySelector('#fileMeta'),status=document.querySelector('#studioStatus'),replace=document.querySelector('#replaceFile'),remove=document.querySelector('#removeFile');let url=null;const allowed=f=>f&&(f.type==='audio/mpeg'||f.type==='audio/wav'||/\.(mp3|wav)$/i.test(f.name));const size=n=>n<1048576?(n/1024).toFixed(1)+' KB':(n/1048576).toFixed(1)+' MB';function clear(){player.pause();player.removeAttribute('src');player.load();if(url)URL.revokeObjectURL(url);url=null;input.value='';panel.hidden=true;drop.hidden=false;status.textContent='Listo para cargar una pista.'}function load(f){if(!allowed(f)){status.textContent='Formato no admitido. Usa MP3 o WAV.';return}if(url)URL.revokeObjectURL(url);url=URL.createObjectURL(f);player.src=url;name.textContent=f.name;meta.textContent=size(f.size)+' · '+(f.type||'audio');panel.hidden=false;drop.hidden=true;status.textContent='Pista preparada. Puedes reproducirla sin subirla al servidor.'}pick.onclick=()=>input.click();replace.onclick=()=>input.click();remove.onclick=clear;input.onchange=()=>load(input.files[0]);['dragenter','dragover'].forEach(e=>drop.addEventListener(e,x=>{x.preventDefault();drop.classList.add('dragging')}));['dragleave','drop'].forEach(e=>drop.addEventListener(e,x=>{x.preventDefault();drop.classList.remove('dragging')}));drop.addEventListener('drop',e=>load(e.dataTransfer.files[0]));drop.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();input.click()}});window.addEventListener('beforeunload',()=>{if(url)URL.revokeObjectURL(url)})})();